package de.tum.cit.ase;

public class Main {
    public static void main(String[] args) {
        /*
        StorageStation storageStation = new StorageStation(2, 3);

        for (int i = 0; i < 5; i++) {
            GhostTrain ghostTrain = new GhostTrain(i, 2, "GhostConductor " + i);
            storageStation.arriveStation(ghostTrain);
        }

        for (int i = 0; i < 6; i++) {
            GhostTrain ghostTrain = storageStation.departStation();
            if (ghostTrain == null) {
                System.out.println("No ghost train could depart.");
            } else {
                System.out.println("Ghost train " + ghostTrain.getId() + " with " + ghostTrain.getAmountOfCars() + " cars and conductor " + ghostTrain.getGhostConductorName() + " departed.");
            }
        }
        */
    }
}
